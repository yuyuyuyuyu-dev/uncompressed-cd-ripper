import { test } from "vitest";

test("should notify the user when an error occurs", async () => {
	// TODO
});

test("should reach the detail screen from the notification", async () => {
	// TODO
});

test("should send the error report from the detail screen", async () => {
	// TODO
});

test("should build the error report from nothing but the error message, the error type, the stack trace, the time it occurred, the application version, the operating system name and version, the architecture and what the user wrote", async () => {
	// TODO
});
